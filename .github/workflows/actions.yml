name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: code
        run: echo "hello world"

      - name: secrets
        env: 
          secret: ${{ secrets.sec }}
        run: echo "Secret is $SECRET"

      - run: echo "hello world from xaviera40404"
